import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    height: "300px",
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
